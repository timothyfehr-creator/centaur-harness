# ILLUSTRATIVE adjudicator-only state — SYNTHETIC; asserts no real-world fact. Only the adjudicator
# sees this; it must never appear in any agent's compiled context. The NUMERIC block thresholds live in
# the typed engine state (the turn records' start_state, built by the drive), not in this prose registry.
schema_version: "1.0"
items:
  - id: adj-001
    statement: "BOTH routes r1 and r2 carry hidden block thresholds known only to the adjudicator (synthetic). Each road is an independent gamble: a block succeeds only if the adjudicator's d100 falls under that road's own hidden threshold."
    label: GAMED_FUTURE
