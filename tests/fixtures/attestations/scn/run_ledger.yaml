# Synthetic run-ledger for the attestation fixtures. The resolver reads only `code_version`;
# this FIXED literal lets valid fixtures pin it and stale fixtures mismatch it, independent of
# the live repo SHA.
schema_version: "1.0"
code_version: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
