# Synthetic run-ledger for the calibration fixtures. The gate reads only `code_version`;
# this FIXED literal lets the valid record pin it and stale fixtures mismatch it,
# independent of the live repo SHA.
schema_version: "1.0"
code_version: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
