# Synthetic CALIBRATED signoff (the default for the calibration-record fixtures): it makes the
# calibration gate REQUIRE a resolving calibration.yaml. The gate reads only calibration_status.
schema_version: "1.0"
id: signoff-001
review_ref: review-001
code_version: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
decision: APPROVED
signed_by: "fixture-signer"
date: "2026-06-22"
calibration_status: CALIBRATED
