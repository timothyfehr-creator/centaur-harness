# Defect: category not in enum  ->  invalid-enum / category
schema_version: "1.0"
events:
  - id: evt-001
    description: "Illustrative synthetic event (not real)."
    category: WEATHER
    confidence: LIKELY
    claims: [claim-001]
