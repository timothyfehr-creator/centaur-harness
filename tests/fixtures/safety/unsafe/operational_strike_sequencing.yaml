# SYNTHETIC unsafe fixture -- placeholder text that TRIGGERS one BROADER-tier safety
# pattern (operational-strike-sequencing) with NO real harmful content. Caught only when
# `broader` is enabled; under the default `conservative` tier this file is clean.
# Concrete tokens are the phase marker, the suppress verb, and the asset noun only.
note: "Phase 2: degrade the <PLACEHOLDER> SAM site"
